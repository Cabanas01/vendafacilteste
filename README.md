# VENDAFÁCIL

Sistema de Ponto de Venda (PDV) inteligente, fácil e integrado com IA.

## 🚀 Como subir o código para o GitHub

Como este é um ambiente de desenvolvimento, você precisa executar os comandos de sincronização manualmente no seu terminal. Siga os passos abaixo:

1. **Inicializar o repositório local (apenas na primeira vez):**
   ```bash
   git init
   ```

2. **Conectar ao repositório remoto:**
   ```bash
   git remote add origin https://github.com/Cabanas01/VENDAFACIL.git
   ```

3. **Adicionar seus arquivos:**
   ```bash
   git add .
   ```

4. **Criar um commit com suas alterações:**
   ```bash
   git commit -m "Implementação de autenticação, faturamento e painel administrativo"
   ```

5. **Enviar os arquivos para o GitHub:**
   ```bash
   git branch -M main
   git push -u origin main
   ```

## 🛠️ Tecnologias Utilizadas

- **Frontend:** Next.js 15 (App Router), React 19, Tailwind CSS.
- **UI Components:** Shadcn/UI, Lucide React.
- **Backend:** Supabase (Auth, Database, Edge Functions).
- **AI:** Google Genkit com Gemini 2.5 Flash.
- **Pagamentos:** Integração com Hotmart.

---
*Desenvolvido com Firebase Studio.*